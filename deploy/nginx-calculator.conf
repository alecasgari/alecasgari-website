# calculator.alecasgari.com — serve in-repo calculator/ as document root
#
# Live setup (NPM + Docker):
#   Proxy Host: calculator.alecasgari.com → http://calculator-web:80
#   Container mount:
#     /home/alecadmin/alecasgari-website/calculator → /usr/share/nginx/html
#
# One push to alecasgari-website updates both the main site and the calculator
# (deploy.sh git pull; calculator-web reads the same tree in-place).
#
# On alecasgari.com Advanced: redirect /calculator → subdomain
# (see deploy/nginx-url-redirects.conf).
